version: '3.8'

services:
  lovable-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lovable-radiology-app
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - clinic-network

  # Seus outros serviços (exemplo):
  # n8n:
  #   image: n8nio/n8n
  #   container_name: n8n
  #   ports:
  #     - "5678:5678"
  #   networks:
  #     - clinic-network
  #   volumes:
  #     - n8n_data:/home/node/.n8n
  #   restart: unless-stopped

  # orthanc:
  #   image: jodogne/orthanc-plugins
  #   container_name: orthanc
  #   ports:
  #     - "4242:4242"
  #     - "8042:8042"
  #   networks:
  #     - clinic-network
  #   volumes:
  #     - orthanc_data:/var/lib/orthanc/db
  #   restart: unless-stopped

networks:
  clinic-network:
    driver: bridge

# volumes:
#   n8n_data:
#   orthanc_data:
